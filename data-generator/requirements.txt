# generate.py only uses the Python standard library (csv, random, datetime,
# argparse) — per-row simulation with running state per device suited a
# plain loop better than pandas/numpy vectorised operations.
